'use strict';

let side1: number = 2;
let side2: number = 3;
let side3: number = 4;

console.log("Surface Area: " + (side1 * side2 + side1 * side3 + side2 * side3) * 2);
console.log("Volume: " + side1 * side2 * side3);